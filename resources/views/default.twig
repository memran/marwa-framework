<!DOCTYPE html>
<html lang="en" class="h-full antialiased">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{% block title %}Welcome to MarwaPHP{% endblock %}</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      darkMode: 'class',
      theme: {
        extend: {
          colors: {
            brand: { DEFAULT: '#0ea5e9', dark: '#22d3ee' }
          },
          keyframes: {
            fadeInUp: {
              '0%': { opacity: 0, transform: 'translateY(20px)' },
              '100%': { opacity: 1, transform: 'translateY(0)' }
            },
            typing: {
              '0%': { width: '0ch' },
              '100%': { width: '40ch' }
            },
            blink: {
              '0%, 100%': { borderColor: 'transparent' },
              '50%': { borderColor: 'white' }
            }
          },
          animation: {
            fadeInUp: 'fadeInUp 1s ease-out forwards',
            typing: 'typing 4s steps(40) 1s forwards, blink .75s step-end infinite'
          }
        }
      }
    }
  </script>
  <style>
    .typewriter {
      white-space: nowrap;
      overflow: hidden;
      border-right: 3px solid white;
    }
  </style>
</head>
<body class="min-h-full bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 text-white">
  {% block content %}{% endblock %}
</body>
</html>
